const enum NamesClients {
  UNKNOWN = 'unknown',
  ANONYMOUS = 'anonymous',
  PASSWORD = 'password',
  EXISTING = 'existing',
}
export default NamesClients;

export const CurrencyCode = {
  EUR: '€',
  USD: '$',
};

export const enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

export const LIMIT = 6;
